import sncosmo
import triangle
from astropy.table import Table
import math
import matplotlib.pyplot as plt
import matplotlib.mlab as mlab
import numpy as np
import collections
import lightcurve_analysis as lca

class LCA_packet(object):
    "class to generate packet of all the fits I want for extinction analysis"
    def __init__(self, sn_id, model, obstable):

        # test paramter
        self.word = word

        self.lc_noext_fit_noext
        self.lc_noext_fit_ext
        self.lc_ext_fit_noext
        self.lc_ext_fit_ext

    # test function
    def test(self):
        print "hello," + self.word

    def make_packet(self)

    lca.LCA()
